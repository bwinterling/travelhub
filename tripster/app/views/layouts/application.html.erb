<!DOCTYPE html>
<html>
<head>
  <title>Triptionary</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  <%= favicon_link_tag "favicon.ico", rel: "icon", type: "image/x-icon" %>
</head>
<body>
<div class="header">
  <div id="masthead" >
    <div id= "botmenu"> 
    <div><h5 class="submenu" id='home'> Triptionary Logo </h5></div>
  <div class="submenu" >
    <ul>
      <% if current_user %>
        <li><%= link_to "Logout", logout_path, method: "delete" %></li>
        <li><%=link_to "Add A Trip", new_trip_path %> </li>
        <div>
        <li><%= image_tag(current_user.avatar_url, id: "profile_image") %><t/> <%= current_user.name %></li></div>
      <% else %>
        <li><%= link_to image_tag("sign-in-with-twitter-gray.png", alt: "Login with Twitter"), "/auth/twitter" %></li>
      <% end %>
</div>
 <% if flash[:notice] %>
    <div class="notice"><%= flash[:notice] %></div>
  <% end %>
<%= yield %>

</body>
</html>
