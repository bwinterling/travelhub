<%= link_to "Connect with Instagram", @photos_api.connect_url %>

<div class="modal" aria-hidden="true">
  <div class="modal-body">
    <div class="span6">
      <% @tweets.each do |t|%>
      <%= t.text %><br/>
      <% end %>
    </div>
    </div>
  </div>
</div>
