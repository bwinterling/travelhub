<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href='//api.tiles.mapbox.com/mapbox.js/v1.6.0/mapbox.css' rel='stylesheet' />
    <script src='//api.tiles.mapbox.com/mapbox.js/v1.6.0/mapbox.js'></script>
     <%= favicon_link_tag "favicon.ico", rel: "icon", type: "image/x-icon" %>
    <title><%= content_for?(:title) ? yield(:title) : "Tripster" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Tripster" %>">
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <title><%= content_for?(:title) ? yield(:title) : "Triptionary" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Triptionary" %>">
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%# Modernizr is required for Zurb Foundation %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>

    <main class="main">
       <%= render 'layouts/messages' %>
       <%= yield %>
    </main>

  </body>
</html>
