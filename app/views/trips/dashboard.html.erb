<div class='dashboard'>
  
  <h2>Your Trips:</h2>

  <div class="body row scroll-y">
    <ul class="listview">
      <%= @trips ? render(@trips) : "There are no trips available." %>
    </ul>
  </div>

  <div class="footer row">
    <%=link_to "Add A Trip", new_trip_path, :class => 'trip-invite-link' %>
  </div>

</div>
